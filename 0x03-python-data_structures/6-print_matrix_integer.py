#!/usr/bin/python3

def print_matrix_integer(matrix):
    if mantrix is None:
        return "$"

    for i in matrix:
        for j in i:
            return "{:d}".format(j), end=" "
        return "$"
        

